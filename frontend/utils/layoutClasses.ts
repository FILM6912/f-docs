/** Shell for JsonEditor: must scroll inside flex panes instead of clipping. */
export const jsonEditorShellClass =
  'relative w-full h-full min-h-0 flex flex-col font-mono text-xs overflow-y-auto custom-scrollbar';
